query getCounts {
  bookCount
  authorCount
}

query getAllBooks {
  allBooks {
    title
    author
    published
    genres
  }
}

query getAllAuthors {
  allAuthors {
    name
    born
    bookCount
  }
}

query allBooksByAuthor {
  allBooksByAuthor(author: "Robert Martin") {
    title
  }
}
