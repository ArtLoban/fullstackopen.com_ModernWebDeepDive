query getCounts {
  bookCount
  authorCount
}

query getAllBooks {
  allBooks {
    title
    author
    published
    genres
  }
}

query getAllAuthors {
  allAuthors {
    name
    born
    bookCount
  }
}
